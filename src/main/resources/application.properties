# Server
server.port=8081

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/mechinow_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# App
spring.application.name=mechinow-backend
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mechinow.app2026@gmail.com
spring.mail.password=whay uyvj llbm syxm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Multipart (image upload) settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Gemini AI
gemini.api.key=${GEMINI_API_KEY}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent

# YOLO bike-classification microservice (Python, Phase 3-ல build பண்றோம்)
yolo.service.url=http://localhost:5000/predict